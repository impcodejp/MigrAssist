# 給与・勤怠システム突合ツール 仕様書

**バージョン**: 0.1（ドラフト）  
**作成日**: 2026-06-02  
**ステータス**: 壁打ち中・未確定事項あり

---

## 1. 概要

### 1.1 目的

給与・勤怠システム導入時に、現行システム（他社システム）と新システム（MJSシステム）がそれぞれ出力したCSVファイルを読み込み、指定した項目同士を1レコードずつ突合して差異を検出するデスクトップツール。

### 1.2 技術スタック

| 区分 | 技術 |
|------|------|
| フレームワーク | Tauri |
| フロントエンド | React |
| バックエンド | Rust |

---

## 2. 画面構成

### 2.1 メイン画面（単一画面）

```
┌─────────────────────────────────────────────────────┐
│ ● MJSシステム                    文字コード          │
│ [ファイルパス入力欄          ] [sjis▼] [参照]        │
│                                                     │
│ ● 他社システム                   文字コード          │
│ [ファイルパス入力欄          ] [sjis▼] [参照]        │
│                                                     │
│                          [ファイル取込]              │
│                                                     │
│ ┌─────────────────────────────────────────────────┐ │
│ │ [設定出力] [設定取込]                            │ │
│ │ 突合設定テーブル（後述）                         │ │
│ │ スクロールバー                                   │ │
│ └─────────────────────────────────────────────────┘ │
│                                                     │
│ ● 出力フォルダ                                      │
│ [フォルダパス入力欄              ] [参照]            │
│                                                     │
│                                    [実行]           │
└─────────────────────────────────────────────────────┘
```

---

## 3. 機能仕様

### 3.1 ファイル入力エリア

#### 3.1.1 MJSシステム CSVファイル

| 項目 | 仕様 |
|------|------|
| 入力方式 | テキストボックスへの直接入力、または「参照」ボタンによるファイル選択ダイアログ |
| 文字コード選択 | ドロップダウン（選択肢：`sjis` / `utf-8` / `utf-8-bom`） |
| デフォルト文字コード | `sjis` |
| ファイル形式 | CSV（.csv） |

#### 3.1.2 他社システム CSVファイル

MJSシステムと同様の仕様。

#### 3.1.3 「ファイル取込」ボタン

- 両ファイルのパスが入力済みであることを検証する
- 各ファイルを指定文字コードで読み込み、ヘッダー行を解析する
- 取込成功後、突合設定テーブルを更新する（後述 §3.2）
- エラー時はダイアログでメッセージを表示する

**エラーケース例：**
- ファイルパスが未入力
- ファイルが存在しない
- 指定文字コードで読み込めない
- ヘッダー行が空

---

### 3.2 突合設定テーブル

ファイル取込後に表示されるインタラクティブなテーブル。突合する列の対応関係を定義する。

#### 3.2.1 テーブル構造

```
┌──────────┬────────────┬────────┬────────┬────────┬────────┐
│ 比較対象  │     -      │   レ   │   レ   │   レ   │   レ   │
│ MJSシステム│ 社員コード │出勤日数│欠勤日数│出勤時間│所定内残業│ ...
│ 他社システム│ (▼プルダウン)│(▼)  │(▼)   │(▼)   │(▼)   │ ...
│ 許容誤差± │            │        │        │        │   5    │
└──────────┴────────────┴────────┴────────┴────────┴────────┘
```

#### 3.2.2 行の定義

| 行名 | 内容 |
|------|------|
| **比較対象** | チェックボックス（レ）。ONの列を突合対象にする。キー列（先頭列）は比較対象外（`-` 固定） |
| **MJSシステム** | 取り込んだMJSシステムCSVのヘッダー名をそのまま表示（読み取り専用） |
| **他社システム** | プルダウン。他社システムCSVのヘッダー一覧から選択。MJSシステムと名称が完全一致する場合は初期値として自動選択 |
| **許容誤差±** | 数値入力フィールド（整数）。空欄は `0` として扱う。数値型の列にのみ有効 |

#### 3.2.3 列構成ルール

- **先頭列（キー列）**: MJSシステムCSVの**1列目**を突合キーとして固定で使用する。列名はMJSシステムCSVの1列目ヘッダー名をそのまま表示する。比較対象チェックは常に OFF・変更不可
- **2列目以降（比較列）**: 突合して差異を検出する列。ファイル取込時にMJSシステムCSVのヘッダーに従って自動生成される。列の追加・削除は不可

#### 3.2.4 初期値自動マッチング

ファイル取込後、他社システム行のプルダウンを以下のロジックで初期設定する：

1. MJSシステムのヘッダー名と他社システムのヘッダー名を**完全一致**で比較
2. 一致した場合 → 対応する他社システムのヘッダーを初期値にセット
3. 一致しない場合 → プルダウンは未選択（空白）

---

### 3.3 設定の保存・読み込み

#### 3.3.1 「設定出力」ボタン

- 現在の突合設定テーブルの状態をファイルに保存する
- 保存対象：
  - MJSシステム側の各列ヘッダー名
  - 他社システム側の対応列ヘッダー名
  - 比較対象チェックの状態
  - 許容誤差値
- TODO: 保存ファイル形式（JSON / CSV / 独自形式）は未確定

#### 3.3.2 「設定取込」ボタン

- 保存済みの設定ファイルを読み込み、突合設定テーブルを復元する
- 読み込んだ設定のヘッダー名と現在取り込み済みCSVのヘッダーが不一致の場合は警告を表示する

---

### 3.4 出力フォルダ

| 項目 | 仕様 |
|------|------|
| 入力方式 | テキストボックスへの直接入力、または「参照」ボタンによるフォルダ選択ダイアログ |
| 用途 | 比較結果ファイルの出力先 |

---

### 3.5 「実行」ボタン

#### 3.5.1 事前バリデーション

以下をすべて満たさない場合はエラーダイアログを表示して処理を中断する：

1. MJS・他社システムのCSVがファイル取込済み
2. キー列の他社システム側プルダウンが選択済み
3. 比較対象チェックがONの列が1件以上存在する
4. 比較対象ONの列すべてで他社システム側プルダウンが選択済み
5. 出力フォルダが指定済み

#### 3.5.2 突合処理ロジック（Rustバックエンド）

```
1. MJSシステムCSVをキー列の値をキーとしてHashMapに格納
2. 他社システムCSVを1行ずつ読み込む
3. 他社システムのキー列の値でHashMapを検索
   - 一致レコードあり → 各比較列を突合
   - 一致レコードなし → 「MJS側に該当なし」として記録
4. MJS側にしか存在しないレコードを「他社側に該当なし」として記録
5. 比較列ごとの差異チェック：
   - 許容誤差が0のとき：文字列として完全一致チェック
   - 許容誤差がN（>0）のとき：数値変換後に絶対差 ≤ N で一致とみなす
6. 結果をCSVに出力
```

#### 3.5.3 出力ファイル

出力フォルダに以下2ファイルを生成する。タイムスタンプは実行日時（ローカル時刻）。

---

##### ① サマリー比較_yyyymmddhhmm.csv

比較列ごとに1行、全体集計を出力する。

| 列名 | 内容 |
|------|------|
| 項目名 | MJSシステム側のヘッダー名 |
| 結果 | 不一致件数が0件の場合 `〇`、1件以上の場合は不一致件数（数値） |

**出力例：**

```
項目名,結果
出勤日数,〇
欠勤日数,3
出勤時間,〇
所定内残業,12
残業時間,〇
```

**補足：**
- キー列（社員コード等）は出力しない
- 片側のみ存在レコード（MJS側のみ／他社側のみ）は別途集計行として末尾に追加する

```
項目名,結果
...（比較列）...
MJS側のみ存在,2
他社側のみ存在,0
```

---

##### ② 明細比較_yyyymmddhhmm.csv

不一致があった比較列ごとにセクションを分けて出力する。

**構造：**
- 不一致が1件以上あった比較列についてのみ出力する（一致した列はスキップ）
- 各セクションはヘッダー行 → データ行の順

**セクションのヘッダー行：**

```
項目名：出勤日数
```

**セクションのデータ行：**

| 列名 | 内容 |
|------|------|
| キー値（MJS1列目のヘッダー名） | MJSシステムCSVの1列目ヘッダー名をそのまま列名として使用。値は突合キーの値 |
| MJS値 | MJSシステム側の該当列の値 |
| 他社値 | 他社システム側の該当列の値 |
| 差異 | MJS値 − 他社値（数値列のみ。文字列列は空欄） |

**出力例：**

```
項目名：欠勤日数
社員コード,MJS値,他社値,差異
1001,2,0,2
1045,1,3,-2
1089,0,1,-1

項目名：所定内残業
社員コード,MJS値,他社値,差異
1001,20,25,-5
...
```

**補足：**
- 片側のみ存在レコードも対象列すべてに対して出力する（他社値またはMJS値は空欄）
- 許容誤差内の差異は「一致」とみなすため明細には出力しない

---

## 4. 非機能要件

| 項目 | 内容 |
|------|------|
| 対応OS | Windows 11 |
| CSVサイズ上限 | 後回し |
| エラーハンドリング | ユーザーフレンドリーな日本語メッセージを表示 |
| ローカライズ | 日本語のみ |
| プログレス表示 | 実行中は進捗バーを表示する（処理ステップ単位で更新） |

---

## 5. TODO・未確定事項一覧

| # | 項目 | 優先度 |
|---|------|--------|
| 1 | 想定する最大CSVレコード数（性能要件） | 後回し |

---

*本仕様書は壁打ち段階のドラフトです。TODO項目を順次確定させて更新します。*
